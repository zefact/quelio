# 変更履歴

Quelio の主な変更点を記録します。
リリース時には、タグ (例: `v0.2.0`) に対応するバージョンのセクションが
そのまま GitHub Releases の説明文になります。

書き方: 開発中は「未リリース」に書き溜め、リリース時にその内容を
新しいバージョンの見出しに移してからタグを打ちます。

## [0.8.1] - 2026-09-04

SQL関数のリファレンスを追加し、大きな結果の取り出しを速くしました。

### 追加

- **SQL関数のリファレンス** (MySQL 121件 / PostgreSQL 103件)。
  SQLエディタの「関数」ボタン、右クリックメニュー、または ⌘⇧H で開きます
  - 名前を覚えていなくても「切り捨て」「前ゼロ」「月末」といった言葉で探せます
  - 1件ずつに、そのまま実行できる例とその結果、間違えやすい所を添えてあります
    (例と結果は実際のMySQL・PostgreSQLで流して確かめた202件です)
  - 選んだ関数は、その場でエディタのカーソル位置へ差し込めます
- 入力補完に関数を混ぜました。`date_` まで打つと DATE_FORMAT が候補に出て、
  横に書式・例・結果が並びます。選ぶと `DATE_FORMAT(日時, 書式)` の形で入り、
  引数の所は Tab で渡り歩けます

### 変更

- 大きな結果の取り出しを速くしました (20万行×10列での計測)
  - 結果をCSVエディタで開く: 約2.1秒 → **0.6秒**。
    CSVに書いて一時ファイルへ落とし、読み直して解析する往復をやめました
    (DBのデータが一時フォルダに残らなくなります)
  - CSV出力: 約1.9秒 → **0.7秒**
  - 一番の重荷は値を文字列にする所でした (PostgreSQLで1.5秒 → 0.27秒)。
    型を順に試していたのを、列の型を見て一度で決めるようにしています。
    見覚えのない型は今までどおり順に試すので、出る値は変わりません

### 修正

- 結果をCSVエディタで開いたとき、`=` で始まる値の先頭に `'` が
  付いてしまっていたのを直しました

## [0.8.0] - 2026-09-04

CSVエディタ (QuelioCSV) を別ウィンドウとして追加しました。
**まだ開発途中の機能です** (名乗りの横にβと出ます)。ひととおり使えますが、
操作や見た目はこれから変わることがあります。おかしな所は遠慮なく知らせてください。

### 追加

- **CSVエディタ (β)**。上部のアイコン、⌘K、ようこその画面、
  クエリ結果のボタンから開けます (結果からは画面の1000行ではなく全件。
  時間が掛かるときは進捗が出て、途中で止められます)
  - 文字コード・改行・区切り・引用符は、開いたときの形のまま保存します
    (Shift_JIS / BOM / CRLF / タブ区切り。触っていない行はそのまま残ります)
  - 前ゼロや大きなIDを数値に変えないので、Excelで開いたときのような崩れが出ません
  - 10万行でも引っかからないよう、見えている行だけを描いています
  - 検索・置換 (⌘F)、行と列の追加・削除、列名の変更、取り消し・やり直し
  - 複数セルの選択 (ドラッグ / Shift / 行番号クリック / ⌘+クリックで離れた所も)。
    2つ以上選ぶと、情報バーに合計 (数値だけのとき) か中身の入ったセルの数を出します
  - ⌘+矢印でその列・行の端まで、Ctrl+矢印でデータの切れ目まで移動します
  - 左右の分割表示。スクロールを合わせる・合わせないを選べ、仕切りは動かせます
  - 固定長ファイルの読み書き。桁は自動で推測し、Shift_JISのバイト数でも数えられます。
    寄せと埋め文字も見分け、桁に入り切らない値があれば保存せずに知らせます。
    桁の並びは名前を付けて残せます
  - 2つのCSVの比較。キーの列で突き合わせ、違うセルだけに色を付けて飛べます
  - Excel (.xlsx) での書き出し
- SQLエディタ: 実行ボタンの ▾ で「部分実行 / 全体実行」を選べるようにしました
  (シートごとに覚えます)。部分実行では、走る文に帯を敷き、
  ボタンにも「この文を実行 (2/3)」と出るので、押す前に範囲が分かります
- SQLエディタ: 複数行を選んで Tab で字下げ・Shift+Tab で戻す、行番号を押して行選択、
  .sql への保存 (⌘⇧S)、タブの右クリックメニュー、
  WITH句・導出表・副問い合わせの中での入力補完
- SQLエディタ: 1000行ずつのページングで、「件数」ボタンから全体の件数を数えられます
- シートの見出しに、先頭のコメント (`-- 月次の売上` など) を使うようにしました
- 名乗りを「QuelioDB」「QuelioCSV」に分けました (どちらの窓か一目で分かるように)

### 変更

- 結果の持ち出しボタンを「選ぶ」と「動かす」に分けました。▾ では
  CSV / Excel / CSVエディタで開く を選ぶだけで、動くのはボタンを押したときです
  (選んだ先は次回の既定になります)

### 削除

- 結果のピン留め (前の結果を取っておいて横に並べる) をやめました。
  結果を全件CSVエディタで開けるようになり、そちらでキーを決めた突き合わせや
  セル単位の色分けができるためです (グラフはそのまま残ります)

### 修正

- 別ウィンドウを開くボタン (コンソール・CSVエディタなど) を押したあと、
  ヒントが出しっぱなしになっていたのを直しました
- データタブで「行を追加」を押しても入力欄が見つからないことがあったのを直しました。
  一覧は200行ずつしか描いていないため、行数の多い表では入力欄が画面の外にありました

## [0.7.3] - 2026-09-03

SQL結果のExcel出力を追加し、日時を手元のタイムゾーンで扱うようにしました。

### 追加

- SQL結果を Excel (.xlsx) でも書き出せるようにしました。結果ヘッダのボタンから
  CSV / Excel を選べます (選んだ形式は次回の既定になります)。
  見出しの色付け・絞り込み・見出し行の固定つきで、数値と日付はExcelの型で書きます
  (前ゼロのコードや大きなIDは文字列のまま残します)

### 変更

- 上部のボタンを整理しました。検索・ER図・スキーマ・関数の定義を「ツール」に
  まとめ、分かりにくかった呼び名を直しています
  (「一覧」→「スキーマ」/「定義」→「関数・手続」/「実行中」→「プロセス一覧」)
- データタブの「再読込」を「適用」に一本化し、「行を追加」を右側へ離しました
- データタブの絞り込み条件を、テーブルごとに覚えておくようにしました
  (別のテーブルを見てから戻っても入れ直さずに済みます。アプリを閉じると消えます)

### 修正

- 日時が9時間ずれて見えていたのを直しました。ドライバがUTCに固定していたため、
  テーブルの作成・更新日時も TIMESTAMP列も、`mysql` コマンドや psql と
  違う時刻で出ていました。手元のタイムゾーンに合わせます
- データタブの「条件を作る」で、カラムの型を見て引用符を付けるようにしました。
  `varchar` の "0123" が数値の 123 になり、PostgreSQLでは型が合わず
  エラーになっていました

## [0.7.2] - 2026-09-02

テーブル定義書のExcelを日本の様式に作り直し、TLSまわりの守りを固めました。

### 追加

- 定義書のExcelを日本の定義書の様式に刷新しました。表紙に文書情報と改訂履歴の欄、
  カラム一覧は No / PK / UQ / FK / 論理名 / 物理名 / データ型 / 桁 / 小数 /
  必須 / 既定値 / 自動採番 / 備考 の13列に。配色・罫線・印刷の設定も見直しました
- 証明書を検証しない接続に注意を出すようにしました。接続設定のTLS欄と、
  画面下の帯 (「TLS未検証」) に出ます。手元のDBとSSH/SSM経由には出しません

### 変更

- 色の選択肢を8色から12色に増やし、灰色を足しました
  (接続・フォルダ・ER図で共通。今まで選べた色はそのまま残しています)
- 作成するインストーラを macOS / Windows のものだけに限定しました
  (保存時暗号化の鍵を置くOSのキーチェーンが、この2つしか対応していないため)

### 修正

- エクスポート・インポート (mysqldump / pg_dump / mysql / psql) に、
  接続設定のTLSを引き継ぐようにしました。これまではダンプのときだけ
  ツールの既定 (証明書を検証しない) に落ちていました。
  MariaDBのクライアントには `--ssl-mode` が無いため、書き方を出し分けます
- データタブの絞り込み条件も、SQLエディタと同じく「取り返しのつかないSQL」の
  確認を通すようにしました (経路によって守りが変わらないようにするため)
- コメントに `\` を含む定義変更 (MySQL) でSQLが壊れていたのを直しました。
  `C:\temp\` のような値で文字列が閉じず、後ろのSQLまで値として読まれていました
- Valkeyコンソールの `AUTH` や `CONFIG SET requirepass` が、
  クエリログにパスワードのまま残っていたのを直しました (伏せて記録します)
- SSM経由の接続で、ホスト名に `"` が入るとAWS CLIへ渡すパラメータが
  壊れていたのを直しました (JSONを組み立てて渡すようにしました)

## [0.7.1] - 2026-09-01

定義書のExcel出力・SSM経由の接続・日本語テストデータ生成を追加しました。

### 追加

- テーブル定義書の Excel (.xlsx) 出力。スキーマ一覧の「Excel定義書」から
  出力するテーブルを選びます (表紙 / 一覧 / テーブルごとのシート)
- AWS Systems Manager (SSM) のポート転送 / Cloud SQL Auth Proxy 経由の接続。
  接続設定の「接続経路」で選びます (踏み台へのSSHを禁じている環境向け)
- 日本語のテストデータ生成 (テーブルの右クリックから)。論理名・カラム名・型から
  氏名・住所・電話番号などを推測して入れます。自動採番はDBに任せ、
  外部キーは参照先にある値から選ぶので整合は崩れません

### 変更

- CSV取り込みの上限行数を 100万行から 1,000万行へ
- 入力補完のカラム候補をテーブルの定義順に (これまでは a→z 順)。主キーには「PK」
- データタブの「条件を作る」で、要らないカラム名の引用符を付けないように
- サイトと「Quelioについて」に MariaDB・Redis 対応を明記

### 修正

- CSVの出力・取り込み中にタブを移って戻ると、進捗と中止ボタンが消えていたのを直しました
- 結果ヘッダの「グラフ」「結果をピン留め」の説明が左側で切れていたのを直しました
- Windowsで等幅の文字が読みにくく、アンダースコアが見えないことがあったのを直しました

## [0.7.0] - 2026-08-31

日々の摩擦を減らす小粒な改善をまとめて入れました。

### 追加

- 未接続のタブに「ようこそ」画面。ピン留めした接続と最近つないだ接続を
  カードで並べ、1クリックで繋げます (☆ でピン留め)
- 接続先が1つも無いときは「サンプルDBで試す」。受注まわりの小さなSQLite
  (5テーブル・1ビュー・外部キーつき) をその場で作って開きます
- ⌘K から設定・ER図・スキーマ一覧・コンソールなどの操作も呼べるようにしました
- よく見るテーブルをピン留めして一覧の先頭にまとめられます (右クリックから)
- データタブに「条件を作る」。列・演算子・値を選ぶと WHERE句を組み立てます
  (できあがる文も見せます)。列ヘッダのメニューからも開けます
- クエリ結果を棒・折れ線・円グラフで見られるようにしました (結果ヘッダの「グラフ」)
- 実行結果を「ピン留め」して取っておき、次の結果と横に並べて比較できます。
  増えた行・消えた行を色分けします
- セルの内容にJSONの「ツリー」表示を追加。行をクリックすると
  その位置のパス ($.items[0].name) をコピーできます
- ER図を SVG で書き出せるようにしました (「テキスト」メニュー)
- ER図のリバースが、読み込むテーブルを選ぶところから始まるようになりました。
  チェックしたテーブルだけを読み直すので、図の他のテーブルはそのまま残ります

### 変更

- 設定「SQLの整形」の字下げが、エディタでの入力 (Tab・改行) にも効くようになりました

### 修正

- 画面下の帯で、DB種別のバッジのはみ出しと、DB名の縦位置のずれを直しました

## [0.6.3] - 2026-08-31

### 追加

- 接続先に「環境」(本番 / ステージング / 開発) を設定できるようにしました。
  タブ・接続一覧・画面下の帯・ツールバーの色が環境に合わせて変わります
  (接続ごとに色を選んでいればそちらが優先)。「本番」は接続時に確認を出し、
  定義の変更 (ALTER・RENAME) の確認を設定で外していても必ず確認します
- ER図を Mermaid / PlantUML のテキストで書き出せるようにしました
  (ツールバーの「テキスト ▾」からコピー / 保存)。
  GitHub や Notion はMermaidをそのまま図にするので、
  リポジトリに置いて差分を見られます

### 変更

- ⌘Enter が「選択部分、無ければカーソルのある文だけ」を実行するようになりました。
  全体の実行は ⌘⇧Enter です。実行した文はエディタ上で一瞬光ります。
  ツールバーの「実行対象 (全体 / 選択)」の切り替えは廃止しました
  (戻し忘れて意図しない範囲が走る・無反応になる、が起きていたため)
- SQLエディタで改行したとき、直前の行の字下げを引き継ぐようにしました
  (これまでは括弧の中以外は行頭に戻っていました)

## [0.6.2] - 2026-08-31

### 追加

- 画面いちばん下に状態表示 (ステータスバー) を出すようにしました。
  接続先・DB・読み取り専用・直近の実行に加え、トランザクションが開いたままなら
  「確定 / 取り消し」を出し、そのまま閉じようとすると確認します
- SQLの結果とデータの表で、数値の列を右寄せ、日付と真偽の値を色分けするようにしました
- セルの内容の画面で、JSON・PHPシリアライズを整形して読めるようにしました
- テーブルの絞り込みを、日本語名 (論理名) でも探せるようにしました

### 修正

- お気に入りで、フォルダと項目を混ぜて並べられるようにしました。
  これまではフォルダが必ず上にまとまるため、項目の下へ動かしても戻ってしまい
  「動かない」ように見えていました (保存する並び順の形が変わります)

## [0.6.1] - 2026-08-28

### 追加

- SQLの整形の書き方を設定できるようにしました (設定 > エディタ > SQLの整形)
  - カンマの位置・キーワードの大文字小文字・字下げ・AND/ORの位置・
    JOINのONの置き方・字下げのスタイルを、見本を見ながら選べます
- 起動時に、前回の書きかけSQL (シートと付けた名前) を戻せるようにしました
  - 設定 > 一般 > 起動時。既定は切。接続先のタブは戻しません
    (以前の形式の保存内容は読み捨てます)
- 実行履歴を消せるようにしました (1件ずつ / すべて)
- お気に入りのフォルダを先に作れるようにしました (名前の変更・中身ごとの削除つき)
- お気に入りを、フォルダも項目もドラッグで移動・並べ替えできるようにしました

### 修正

- ページングが効かないことがあるのを直しました
  (行コメントで終わるSQL / 名前に limit を含むSQL / FETCH FIRST)
- 続けて取得したとき、古い結果が新しい結果を上書きすることがあるのを直しました
- 接続し直している最中の「中止」が、無関係な接続を止めうるのを直しました
- 定義を変えたあと、SQLパラメータの型推測とカラム説明が古いままになるのを直しました
- 打ち切った (タイムアウトした) 接続を、確認せずに次の操作へ使うことがあるのを直しました
- プルダウン (TLSの選択・文字コード等) が、項目を選んでも閉じないのを直しました
- 接続の途中で失敗したときに、確立した接続を閉じずに捨てていたのを直しました
- ER図・スキーマ一覧を別のタブから開き直すと、前の接続・DBのまま表示されるのを直しました
- ER図のドラッグ中に拡大率を変えると、動きがずれるのを直しました
- Valkey: DB番号に数値以外を指定したときの扱いと、DBの数の取得を直しました
- ⌘⇧W でもタブが閉じていたのを直しました
- SQLエディタを広げるボタンを区切り線の横へ移し、
  広げたままでも結果欄の高さを戻せるようにしました
- お気に入りの保存ダイアログが、必要以上に横長だったのを直しました

### セキュリティ

- SQLパラメータの値を入れた後に、文が増えていないか・読み取り専用のままかを
  再確認するようにしました (「数値」に数値以外が入っていれば実行前に止めます)
- 値を入れて初めて「取り返しのつかないSQL」になる場合に、入力画面で警告を出すようにしました
- `WHERE 1=1` のように条件が常に真の UPDATE / DELETE も、実行前の確認の対象にしました
- CSV・SQLダンプ・キャプチャの出力ファイルを、所有者だけが読める権限で作るようにしました
- 保存済みのパスワードを画面へ渡さないようにしました (接続はバックエンドで行います)
- `enc:v1:` で始まるパスワードが、暗号化されずに保存されるのを直しました
- SSH踏み台の初回接続で、ホスト鍵のフィンガープリントを確認してから信頼するようにしました
- TLSの検証をしない設定のときに、接続ログで分かるようにしました

### 変更

- 設定「前回のタブを復元する」は「前回の書きかけSQLを復元する」に変わりました
  (接続タブの復元は取りやめました。既定は切のままです)
- SQLの整形の既定が、JOINのONを次の行に置く形になりました
- ESLint を導入しました (`npm run lint`)
- 内部の整理: 大きなファイルの分割、SQL実行・カタログ取得の共通化、
  エラーの判定を文言から種類へ変更、タブ状態のreducer化、
  描き直しの削減 (テーブル一覧・ER図・結果のコピー) など

## [0.6.0] - 2026-08-28

### 追加

- テーブル名・カラム名・コメント・値の中身から探せる「検索」(ツールバー)
- テーブル作成の画面。カラム・型・主キー・自動採番・デフォルト値・コメント・
  文字コード・照合順序を決めてから作れます
- データベース / スキーマの作成・削除 (文字コードと照合順序を選択、実行前に確認)
- CSV / TSVファイルの取り込み (列の対応付け・重複時の動き・進捗表示・中止)
- 接続ごとの「読み取り専用」。サーバー側でも読み取り専用にします
- MySQL / PostgreSQL のTLS設定 (検証レベルと証明書を接続ごとに指定)
- サーバーで実行中の接続とSQLの一覧 (SQLの中止・接続の切断もできます)
- 外部キーの追加・削除、関数・プロシージャ・トリガの定義表示、CREATE文の表示
- スキーマ差分から ALTER 文を作る機能
- 1つの接続で複数のSQLシートを持てるようにしました
- タブ操作のショートカット (⌘T / ⌘W / ⌘1〜9 / ⌘K / ⌘F / ⌘⇧F / ⌘/)
- コンソール: 接続情報の記録、行のコピー、SQLログのファイル出力
- Valkey: キーの検索と一括削除
- 長いセルの全文表示・コピー、結果の JSON / Markdown / INSERT 文コピー
- 「Quelioについて」画面

### 変更

- 起動時は空のタブ1つで始まります
  (設定 > 一般 の「前回のタブを復元する」で元の動きに戻せます)
- 「接続」では接続先を保存しなくなりました (保存は「保存」ボタンのときだけ)
- 名前の検索範囲を、選んでいるデータベースの中だけにそろえました
- テーブルの複数選択を、右クリックメニューからもまとめて使えるようにしました
- 取り返しのつかないSQL (DROP / TRUNCATE / WHERE の無い UPDATE・DELETE など) は
  実行前に確認します (ALTER・RENAME の確認は設定で外せます)
- 接続先・フォルダ・インデックスの削除、カラムの並べ替えにも確認を追加しました
- ER図・スキーマ差分の読み込みを専用接続＋一括取得で速くし、中止できるようにしました
- 結果グリッド・ER図・コンソールの描画を軽くしました

### 修正

- 読み取り専用の判定を、接続先の設定 (`sql_mode` など) を見た共通のSQL解析に
  まとめました。MySQLの実行コメント `/*! … */`、PostgreSQLの `$$ … $$`・
  入れ子コメントなどの読み違いで、更新系のSQLがすり抜ける穴を塞いでいます
- SQLパラメータは判定を済ませてから埋め込むようにしました
  (値によって実行される内容が変わらなくなります)
- SQLiteでも実行中のSQLを中止できるようにし、「中止」がサーバー側にも届くようにしました
- 切断・接続先の切り替えで、動いていたCSV取り込みなどが止まらなかったのを直しました
- ROLLBACKの失敗や、自分で書いた BEGIN によって、
  取り消したはずの変更が確定してしまう可能性を無くしました
- 設定ファイルは書き切ってから置き換えるようにし、所有者だけが読める権限で作ります
  (接続先は `.bak` も残します)
- 保存したパスワードが読めなくなる・空で上書きされることがあるのを直しました
- CSV出力とコピーで、値が表計算ソフトの数式として実行されないようにしました
- ファイル名や外部コマンドへ渡す名前 (DB名・テーブル名) の扱いを厳しくしました
- 日本語入力の変換中の Enter / Esc が、操作として扱われるのを直しました
- 実行計画のCSV出力で、計画ではなくデータが保存されるのを直しました
- モーダルの中の右クリックメニュー・プルダウン・入力欄など、
  見た目と操作の不具合を多数直しました

## [0.5.3] - 2026-08-20

### 追加

- SQLエディタと結果欄の境目 (仕切りの右端) に、エディタを画面いっぱいに広げるボタンを追加しました
- 設定の「アップデート」に、公式サイトと機能詳細ページへのリンクを追加しました

### 修正

- 右クリックメニューやドロップダウン、ヒントテキストが画面の外にはみ出して
  見えなくなることがあるのを直しました
- 軽微な問題を修正しました

## [0.5.2] - 2026-08-20

### 追加

- Valkeyのキーと値を画面から編集できるようになりました
  - 値ビューの行をダブルクリックで書き換え、右クリックで要素を削除、
    「+ 要素を追加」で追加できます
    (hash / list / set / zset / stream に対応。stream は仕様上、追加と削除のみ)
  - キー名の変更 (RENAMENX)、TTLの変更 (EXPIRE / PERSIST)、
    キーの削除 (確認あり)、キーの新規作成ができます
  - 反映はEnter、取り消しはEsc (入力欄からフォーカスが外れていてもEscで取り消せます)
  - 値の入力欄は複数行に対応しました (Shift+Enterで改行、Enterで反映)
  - stringの更新は KEEPTTL を付けるので、値を書き換えてもTTLは消えません
  - listは書き換え前に LINDEX で中身を確認し、他で更新されていたら中止します
  - バイナリの値や、長すぎて先頭しか読めていない値は、壊さないよう編集できません
  - string型は値がひとつなので、値ビューは1列だけ表示します

### 修正

- Valkeyのタブを切り替えると、キー一覧や選択中のキーが初期化される問題を直しました
  (別のValkeyタブの内容が表示されることがある問題も併せて修正)
- Valkeyのキー一覧で、型バッジ (STREAM / STRING) がキー名と重なったり、
  キー名の位置が縦にそろわないのを直しました
- Valkeyの値ビューで、最後の行の下に区切り線を表示するようにしました

## [0.5.1] - 2026-08-19

### 変更

- 軽微な問題を修正しました。

## [0.5.0] - 2026-08-19

### 追加

- SQLエディタに入力補完 (オートコンプリート) を追加しました
- データタブでデータを直接編集できるようになりました
- インデックスの追加・変更・削除ができるようになりました
- テーブル定義のカラムを、一覧の行をその場で編集して変更できるようになりました
- カラムの型を、DBで使えるものの一覧から選ぶようにしました
  - MySQL・SQLiteは仕様で決まっている型の一覧、
    PostgreSQLは接続先の pg_type から取得します
    (ユーザー定義のenumやドメイン、PostGISのgeometryなど拡張の型も候補に出ます)

### 変更

- 実行計画 (EXPLAIN / EXPLAIN ANALYZE) が途中で切れないようにしました

## [0.4.4] - 2026-08-18

### 追加

- SQLiteに対応しました
  - 接続設定でSQLiteを選び、データベースファイルを指定すると接続できます
    (サーバー不要。ファイル選択ダイアログから選べます)
  - テーブル/ビュー一覧・カラム・インデックス・外部キー (ER図) の表示、
    データタブ、SQL実行、CSVダウンロードに対応しています
  - 誤操作を防ぐため、存在しないファイルを指定した場合は新規作成せずエラーにします
  - 実行計画は EXPLAIN QUERY PLAN を使います
    (SQLiteに EXPLAIN ANALYZE は無いため、種類の切り替えは表示しません)
  - 外部ツールを使うエクスポート/インポート、実行中SQLのキャンセルは対象外です
- テーブル一覧に再読み込みボタンを追加しました
  - 「テーブル」見出しの横のアイコンで一覧を取得し直せます。
    選択中のテーブルと表示中の内容はそのまま維持します
  - Valkeyのキー一覧にも同じ位置に再読み込みボタンを追加しました

### 変更

- 接続一覧で、フォルダと接続を同じ階層で自由に並べ替えられるようにしました
  - 接続をフォルダより上や、フォルダとフォルダの間にも置けます
  - フォルダ行は上端/下端に落とすとフォルダの前後、中央に落とすとフォルダの中へ入ります
  - フォルダ配下の接続はインデントとガイド線を付けて階層を分かりやすくしました
- Valkeyのキー一覧の下部ボタンを「続きを読み込む」に変更しました
  (すべて読み込み済みのときは無効表示。先頭からの取り直しは見出し横の再読み込みへ)
- Valkeyのキー件数の表示位置を、テーブル一覧と同じ左寄せに揃えました
- 実行・EXPLAIN・履歴などのボタンの高さを揃えました

## [0.4.3] - 2026-08-17

### 追加

- SQL実行結果に「CSVダウンロード」を追加しました (結果タブごと)
  - 画面は1000行ずつの表示ですが、CSVは**そのSQLの全件**を出力します
  - 出力中は書き出し済みの行数と経過時間を表示し、キャンセルできます。
    中止した場合は作りかけのファイルを残しません
  - 並び替え中の結果は、同じ並び順で出力します
  - 文字列・日時はダブルクォートで囲むため、値に改行やカンマ・引用符が
    含まれていても崩れません。数値は囲まず、NULLは空欄で出力します
  - 保存先は設定の「保存先フォルダ」です (未設定ならダウンロードフォルダ)
  - 出力中もメモリ使用量は一定です (1行ずつファイルへ書き出します)

### 変更

- SQL実行結果の件数表示とページ送り (‹ ›) を、結果タブと同じ行へ移動しました
  - どちらも結果タブごとの情報のため、結果のすぐ上で確認できるようにしました

## [0.4.2] - 2026-08-17

### 追加

- テーブルを選ぶと「定義 / データ」のタブで表示するようにしました
  - 「データ」タブでテーブルの中身をそのまま確認できます。SQL実行と同じく
    1000件ごとのページング (‹ › ボタン) に対応しています
  - 絞り込み条件 (WHERE句) を入力して表示を絞り込めます
  - 選んでいたタブはテーブルを切り替えても維持されます
- 結果グリッドの列ヘッダをクリックすると、並び替えメニュー
  (昇順 / 降順 / ソートなし) を開けるようにしました (データタブ・SQL実行結果)
  - 並び替え中の列にはヘッダへ方向マーク (▲▼) を表示します
- 列ヘッダにカーソルを合わせると、カラムの論理名・補足・型を表示するようにしました
  - SQL実行結果では、接続中DBの定義からコメントのあるカラムを引き当てて表示します
- データ・SQL実行結果で行を選択できるようにしました (データタブ・SQL実行結果)
  - クリックで選択、⌘/Ctrl+クリックで追加・解除、Shift+クリックで範囲選択、
    ⌘/Ctrl+Aで全選択ができます
  - ⌘/Ctrl+Cで、選択行 (未選択なら表示中の全行) をタブ区切りでコピーします。
    Excelなどにそのまま貼り付けられます
  - 右クリックすると「データをコピー」「ヘッダー付きでコピー」を選べます

### 変更

- データ・SQL実行結果・EXPLAIN結果の列幅を、内容に合わせて自動調整するようにしました
- 行番号列を「No / 行」ヘッダ＋淡色の区切り表示にして、データの値と区別しやすくしました
  - テーブル定義のカラム・インデックス一覧にも行番号を追加しました
- テーブル定義のインデックス一覧で、主キー (PRIMARY) を先頭に表示するようにしました
- メニューの選択中項目の色を、DB選択メニューなどと同じ表示に統一しました
- 大量データ取得時のメモリ使用量を抑えました
  - 結果セットをストリームで読み、1ページ分 (1000行) を超えた時点で読み取りを
    打ち切るようにしました。`LIMIT` を含むSQLなど自動ページングが効かない場合でも、
    アプリが保持する行数は常に1ページ分に収まります
  - 1セルが長すぎる値 (巨大なTEXT/JSON列など) は先頭1000文字までにし、
    末尾に「… (全N文字)」を付けて表示するようにしました

### 修正

- 設定の「保存先フォルダ」が反映されない問題を修正しました
  - スキーマCSV出力・実行結果キャプチャ(PNG)・SQLエクスポートが、設定に関わらず
    OSのダウンロードフォルダへ保存されていました
- 縦横のスクロールバーが交わる部分に白い四角が表示される問題を修正しました

## [0.4.1] - 2026-08-17

### 追加

- SQLパラメータに対応しました
  - SQL中の `:name` / `@name` をパラメータとして認識し、実行時に入力モーダルで
    値を設定できます。値は保存され、次回から同名パラメータの初期値になります
  - 埋め込み型 (文字列/数値) は比較対象カラムの定義から自動判定します。
    モーダルのプルダウンで明示指定もでき、選んだ型も記憶されます
  - 文字列・コメント内や `::キャスト`・`@@システム変数` は誤検出しません
- 設定に「エクスポート/インポート」を追加しました
  - 接続一覧 (フォルダ構成込み) をJSONで書き出し/取り込みできます。
    セキュリティのため、パスワード・SSHパスフレーズは出力に含まれません
  - 保存済みの全ER図をJSONで書き出し/取り込みできます (同名は上書き)

### 変更

- 実行モード・EXPLAIN・履歴/お気に入りなどのメニューが同時に複数開いて
  しまう問題を修正しました (どれかを開くと他は閉じます)
- モーダル内のプルダウンが外側クリックで閉じない問題を修正しました

## [0.4.0] - 2026-08-17

### 追加

- Valkey (Redis互換のKVS) に対応しました
  - キーブラウザ: SCANによる段階読み込み・パターン絞り込み・型バッジ・TTL表示。
    DB番号 (0-15) はツールバーから切り替えられます
  - キー詳細: 型ごとの値プレビュー (string / hash / list / set / zset / stream) と
    TTL・サイズ・メモリの表示。JSONやPHPシリアライズ (Laravelセッション等) の
    値は「整形」ボタンで階層表示できます
  - コマンドコンソール: 行番号付きエディタで複数コマンドを一括実行。
    結果はコマンドごとのタブに分かれ、履歴・お気に入り・PNGキャプチャも
    SQLと同様に使えます。FLUSHALL等の破壊的コマンドは実行前に確認します
  - TLS接続に対応しました (AWS ElastiCache Serverless等のin-transit暗号化)。
    SSHトンネル経由でも本来のホスト名でSNI・証明書検証を行います
- SSH秘密鍵の「参照」が ~/.ssh フォルダを直接開くようにしました
- 接続先の並び替えで、項目の下半分へのドロップで「後ろに挿入」できるように
  しました (フォルダ内の一番下にも配置できます)

### 変更

- 「保存SQL」の名称を「お気に入り」に変更しました。フォルダ階層は
  閉じた状態で開くようにし、開閉の三角も見やすくしました
- 実行・EXPLAIN・履歴・お気に入りなどのメニューやツールチップが
  実行結果の裏に隠れて見えなくなる問題を修正しました
- SSH踏み台から接続先に到達できない場合に、接続リセットではなく
  具体的な理由を表示するようにしました
- 接続フォームを中央寄せにし、スクロールバーを右端に出すようにしました
- 情報チップ等で和文と英数字の縦位置がずれて見える問題を修正しました

## [0.3.1] - 2026-08-14

### 変更

- 軽微な問題を修正しました。

## [0.3.0] - 2026-08-14

### 追加

- ER図機能を追加しました
  - 「リバース」でDBからテーブルとリレーション (外部キー制約+命名からの推測) を
    読み込んで作図します。再リバース時は配置を維持したまま更新でき、
    新規テーブルの追加や削除済みテーブルの復活も選べます
  - 図は名前を付けて保存でき、接続プロファイルをまたいで開けます。
    1つの図の中にタブで複数のER図を作成でき、タブ名の変更・並べ替えにも対応
  - テーブルのドラッグ配置・幅調整・図からの削除に対応。範囲選択 (背景ドラッグ) で
    まとめて移動・削除、⌘/Ctrl+C・Vでタブ間のテーブルコピーができます
  - リレーションはカラム行の●ハンドルをドラッグして接続。ダブルクリックの
    編集パネルで複合キーの対応を設定でき、線種・色・接続位置も変更できます。
    線同士の交差は半円で飛び越えて表示します
  - 注釈枠 (実線/破線/点線・角丸/四角・背景色・前面/背面) とテキスト見出し
    (サイズ・色) を配置でき、ダブルクリックでその場で編集できます
  - 現在のカラーモードに合わせた配色でPNG出力できます
- ページ内検索 (⌘/Ctrl+F) を改善しました。全ての一致をハイライトし、
  Enter/↑↓で次の一致へ移動します (ER図では表示位置が自動で追従します)

### 変更

- 削除操作 (タブ・テーブル・線・枠・図) の前に確認ダイアログを出すようにしました
- 各種通知を右上のトースト表示に変更しました

## [0.2.5] - 2026-08-13

### 追加

- SQL実行履歴を追加しました (最新100件・再起動後も保持)。
  ツールバーの「履歴・保存」から選ぶとエディタに反映されます
- SQLの保存機能を追加しました。「/」区切りの階層フォルダで整理でき、
  リネーム・フォルダ移動・現在のエディタ内容での上書き保存にも対応しています
- テーブル情報に「文字コード」「AUTO_INCREMENT」を追加しました
  (「更新」は値が取得できない場合も「-」で表示するようにしました)

### 変更

- EXPLAIN ANALYZE のキャプチャを画面と同じツリー表示で出力するようにしました。
  従来の「…(省略)」で切られる制限もなくなり、全ノードが出力されます

## [0.2.4] - 2026-08-12

### 変更

- タブごとのSQL実行を並行化しました。あるタブでSQLを実行中でも、
  他のタブで接続・SQL実行・テーブル閲覧などができます
- 接続・データベース選択のセレクトボックスを独自描画に変更し、
  WindowsでもmacOSと同じ見た目になるようにしました
- 実行計画の操作カラムの幅調整が正しく効かない問題を修正しました
- MySQLでテーブル一覧のツールチップの先頭に「.」が付く問題を修正しました

## [0.2.3] - 2026-08-12

### 変更

- 軽微な問題を修正しました。

## [0.2.2] - 2026-08-12

### 追加

- SQL実行のタイムアウトを設定画面 (一般タブ) から変更できるようにしました。
  (既定60秒、0で無制限)

### 変更

- 軽微な問題を修正しました。

## [0.2.1] - 2026-08-12

### 変更

- 軽微な問題を修正しました。

## [0.2.0] - 2026-08-10

### 変更

- macOS版にDeveloper IDコード署名とAppleの公証 (Notarization) を導入しました。

## [0.1.0] - 2026-08-08

### 追加

- 初回ベータリリース
- MySQL / PostgreSQL への接続 (SSH踏み台トンネル・鍵認証対応)
- 接続プロファイル管理
- タブ式セッション、テーブル構造表示 (カラム / インデックス / テーブル情報)
- SQLエディタ (シンタックスハイライト・整形・複数SQL実行・1000行ページング・サーバーサイドソート)
- EXPLAIN / EXPLAIN ANALYZE の実行計画ビュー
- 実行結果のPNGキャプチャ、SQLコンソール (実行履歴)
- スキーマ一覧・定義書CSV出力、スキーマ差分ビューア
- テーブル選択式エクスポート / SQLインポート (mysqldump / pg_dump / mysql / psql 連携)
- 接続のキープアライブ (定期ping) と、切断時の自動再接続 (SSHトンネルも再構築)
- 自動アップデート機能 (起動時の更新通知バナー / 設定画面からの手動確認)
