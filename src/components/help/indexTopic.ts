/**
 * ヘルプ: インデックス。
 *
 * 考え方は同じでも、作れる種類・効く条件・落とし穴はDBごとに違う。
 * 入口だけ共通にして、あとはDBごとに分けて書く。
 * 「言葉と意味」が並ぶところは表にする
 */
import type { HelpSection, HelpTopic } from "./helpTypes";

/** どのDBでも同じ、いちばん最初の話 */
const INTRO: HelpSection = {
  title: "インデックスとは",
  lines: [
    "本の巻末にある索引と同じで、「この値はどの行にあるか」を先に並べておく仕組みです。",
    "あると、探す・並べ替える・結合するのが速くなります。代わりに、書き込み (INSERT / UPDATE / DELETE) のたびに索引も直すので少し遅くなり、ディスクも使います。",
    "「片っ端から読む」より「索引を引く」ほうが速いのは、目当ての行が全体のごく一部のときです。全体の何割も返すSQLでは、全件読みのほうが速いこともあります。",
  ],
};

// ---------- MySQL / MariaDB ----------

const MYSQL: HelpSection[] = [
  INTRO,
  {
    title: "作れる種類",
    table: {
      head: ["種類", "書き方と意味"],
      rows: [
        [
          "PRIMARY KEY",
          "主キー。表に1つだけ。InnoDBではこれが行そのものの並び順になる (下の「InnoDBの仕組み」)",
        ],
        ["UNIQUE", "重複を許さない索引。NULL は何行あってもよい"],
        ["KEY / INDEX", "ふつうの索引。重複してよい"],
        [
          "複合 (col1, col2)",
          "複数の列をまとめた索引。列の順番がすべて (下の「列の順番」)",
        ],
        [
          "前置き col(20)",
          "文字列の先頭だけを索引にする。TEXT・BLOB には長さの指定が要る (MariaDB は指定しないと自動で切り詰めます)",
        ],
        ["FULLTEXT", "文章の中の語を探す索引。MATCH ... AGAINST で使う"],
        ["SPATIAL", "位置情報 (GEOMETRY) 用の索引"],
        [
          "降順 (col DESC)",
          "大きい順に並べた索引。ORDER BY ... DESC の並べ替えを省ける (MySQL 8 / MariaDB 10.8 以降)",
        ],
        [
          "関数 ((col * 2))",
          "式そのものを索引にする。MySQL 8.0.13 以降。MariaDB にはこの書き方が無く、生成列を作ってそこに索引を張ります",
        ],
        [
          "使わせない指定",
          "消さずに「使わない」状態にして試せる。MySQL は INVISIBLE、MariaDB は IGNORED",
        ],
      ],
    },
  },
  {
    title: "複合インデックスの列の順番",
    lines: [
      "複合インデックスは「左から順に」しか使えません (左端接頭辞)。(a, b, c) の索引は、a / a,b / a,b,c では使えますが、b だけ・c だけでは使えません。",
      "並べる順番は「等値で使う列 → 範囲で使う列 → 並べ替えに使う列」です。範囲 (> や BETWEEN) を使った列より後ろは、絞り込みには使えなくなります。",
    ],
    table: {
      head: ["(a, b, c) の索引に対して", "効き方"],
      rows: [
        ["WHERE a = 1 AND b = 2 AND c = 3", "3列とも使える。いちばん良い形"],
        ["WHERE a = 1 AND b = 2", "a と b まで使える"],
        [
          "WHERE a = 1 AND b > 5 AND c = 3",
          "a と b まで。b が範囲なので、c は絞り込みには使えない",
        ],
        ["WHERE b = 2", "使えない。左端の a が無い"],
        [
          "WHERE a = 1 ORDER BY b",
          "a で絞り、b の並びをそのまま使える (並べ替えが要らない)",
        ],
      ],
    },
  },
  {
    title: "InnoDBの仕組み (知っておくと得)",
    table: {
      head: ["こと", "意味"],
      rows: [
        [
          "主キーが行の置き場所",
          "InnoDBは主キーの順に行そのものを並べて持ちます (クラスタ索引)。主キーで引くのがいちばん速い",
        ],
        [
          "二次索引には主キーが入る",
          "主キー以外の索引は「値 → 主キー」を持ちます。索引を引いたあと、主キーでもう一度行を取りに行きます",
        ],
        [
          "だから主キーは短く",
          "主キーが長いと、すべての二次索引がそのぶん太ります。UUID を文字列で主キーにすると特に効きます",
        ],
        [
          "カバリングインデックス",
          "要る列が索引の中で全部そろえば、行を取りに行かずに済みます。EXPLAIN の Extra に Using index と出ます",
        ],
        [
          "外部キーには索引が自動で付く",
          "MySQL / MariaDB は、外部キーの列に索引が無ければ自動で作ります (PostgreSQL は作りません)",
        ],
      ],
    },
  },
  {
    title: "索引が効かなくなる書き方",
    table: {
      head: ["書き方", "どうなるか / どう直すか"],
      rows: [
        [
          "WHERE DATE(created_at) = '2026-01-01'",
          "列に関数を掛けると効かない。created_at >= '2026-01-01' AND created_at < '2026-01-02' に書き換える",
        ],
        [
          "WHERE price + 100 > 500",
          "同じく式にすると効かない。price > 400 のように、列だけを残す",
        ],
        [
          "WHERE code = 123 (code は文字列)",
          "型が違うと効かないことがある。'123' と渡して型をそろえる",
        ],
        [
          "WHERE name LIKE '%abc%'",
          "中間一致は効かない。前方一致 'abc%' にするか、FULLTEXT を検討する",
        ],
        [
          "WHERE a = 1 OR b = 2",
          "index_merge か全件読みになりがち。UNION ALL に分けると、それぞれで効くことがある",
        ],
        [
          "違う照合順序の列どうしの比較",
          "索引が使えないうえ、エラーになることもある。照合順序をそろえる",
        ],
      ],
    },
  },
  {
    title: "作りすぎないために",
    table: {
      head: ["よくある無駄", "見分け方"],
      rows: [
        [
          "重なった索引",
          "(a) と (a, b) があるなら (a) は要らない。左端接頭辞で兼ねられる",
        ],
        [
          "使われていない索引",
          "MySQL 8 なら sys.schema_unused_indexes で分かる。書き込みを遅くするだけなので外す",
        ],
        [
          "値の種類が少ない列",
          "フラグのような列 (0/1) は、単独では絞れないので効きにくい。複合の2列目などで使う",
        ],
        [
          "消す前に試す",
          "INVISIBLE (MariaDB は IGNORED) にしておけば、消さずに「無い状態」を試せる",
        ],
      ],
    },
  },
  {
    title: "確かめ方",
    table: {
      head: ["やること", "分かること"],
      rows: [
        [
          "テーブルの「インデックス」タブ",
          "その表の索引の一覧。右クリックから CREATE INDEX 文も見られる",
        ],
        ["EXPLAIN の key", "実際に使われた索引。NULL なら使われていない"],
        [
          "SHOW INDEX の Cardinality",
          "その索引で見分けられる値の種類の数。行数に近いほどよく絞れる",
        ],
        [
          "ANALYZE TABLE",
          "統計を取り直す。EXPLAIN の rows が実際とかけ離れているときに実行する",
        ],
      ],
    },
  },
];

// ---------- PostgreSQL ----------

const POSTGRESQL: HelpSection[] = [
  INTRO,
  {
    title: "索引の種類",
    lines: [
      "何も指定しなければ B-tree です。まずはこれで、特別な用途のときだけ他を選びます。",
    ],
    table: {
      head: ["種類", "向いているもの"],
      rows: [
        [
          "B-tree (既定)",
          "= < > BETWEEN IN、並べ替え、一意制約。ほとんどの用途はこれ",
        ],
        ["Hash", "= だけ。B-tree で足りることが多く、出番は少ない"],
        ["GiST", "位置情報・範囲型・全文検索など、重なりを見る検索"],
        ["SP-GiST", "点や IP アドレスのように、空間を分割して持つと速いもの"],
        [
          "GIN",
          "1つの値の中に複数の要素があるもの。全文検索 (tsvector)、JSONB、配列",
        ],
        [
          "BRIN",
          "値が物理的な並びとそろっている巨大な表 (時系列のログなど)。とても小さく作れる",
        ],
      ],
    },
  },
  {
    title: "B-tree の書き方",
    table: {
      head: ["書き方", "意味"],
      rows: [
        [
          "(a, b, c)",
          "複合索引。左から順にしか使えない (a / a,b / a,b,c)。b だけでは使えない",
        ],
        [
          "(a DESC NULLS LAST)",
          "並び順を指定する。ORDER BY の書き方と合わせると並べ替えを省ける",
        ],
        [
          "... WHERE 条件",
          "部分索引。条件に合う行だけを索引にする。小さく速くなる (例: WHERE deleted_at IS NULL)",
        ],
        [
          "(lower(name))",
          "式索引。WHERE lower(name) = ... と、同じ式で書いたときに使える",
        ],
        [
          "... INCLUDE (c)",
          "索引に列を持たせるだけ (絞り込みには使わない)。Index Only Scan にしたいときに使う (PostgreSQL 11 以降)",
        ],
        [
          "UNIQUE",
          "重複を許さない。制約として付けたいなら、索引ではなく UNIQUE 制約で書くほうが意図が伝わる",
        ],
        [
          "(c text_pattern_ops)",
          "C 以外のロケールのDBで、LIKE 'abc%' の前方一致に索引を使わせる",
        ],
      ],
    },
  },
  {
    title: "索引が効かなくなる書き方",
    table: {
      head: ["書き方", "どうなるか / どう直すか"],
      rows: [
        [
          "WHERE date(created_at) = '2026-01-01'",
          "列に関数を掛けると効かない。範囲の条件に書き換えるか、式索引を作る",
        ],
        [
          "WHERE name LIKE '%abc%'",
          "中間一致は効かない。全文検索 (GIN) か pg_trgm を検討する",
        ],
        [
          "WHERE name LIKE 'abc%'",
          "C 以外のロケールのDBでは、ふつうの索引が使われない。text_pattern_ops の索引を別に作る",
        ],
        [
          "型が違うまま比べる",
          "暗黙の変換で効かなくなることがある。列と同じ型の値を渡す",
        ],
        [
          "外部キーの列",
          "PostgreSQL は外部キーに索引を自動では作りません。親を消すときや結合で遅くなるので、自分で作る",
        ],
      ],
    },
  },
  {
    title: "Index Only Scan にするには",
    lines: [
      "要る列がすべて索引の中にあると、表本体を読まずに済みます (EXPLAIN に Index Only Scan と出ます)。",
      "ただしPostgreSQLは「その行が見えてよいか」を表側で持っているため、更新が多いと結局読みに行きます。EXPLAIN (ANALYZE) の Heap Fetches がその回数です。",
      "Heap Fetches が多いときは、VACUUM (または自動VACUUM) が追いついていないことを疑います。",
      "絞り込みに使わない列は INCLUDE で足すと、索引の並び順を崩さずに済みます。",
    ],
  },
  {
    title: "作るとき・消すときの注意",
    table: {
      head: ["やること", "意味"],
      rows: [
        [
          "CREATE INDEX CONCURRENTLY",
          "表に鍵をかけずに作る。動いているサービスではこちら。時間はかかり、失敗すると壊れた索引が残るので作り直す",
        ],
        [
          "DROP INDEX CONCURRENTLY",
          "同じく、鍵をかけずに消す",
        ],
        [
          "REINDEX",
          "索引を作り直す。OS の照合順序が変わった後や、索引が太ってきたときに使う",
        ],
        [
          "ANALYZE",
          "統計を取り直す。作った直後や、データを大きく入れ替えた後に実行する",
        ],
      ],
    },
  },
  {
    title: "確かめ方",
    table: {
      head: ["やること", "分かること"],
      rows: [
        [
          "テーブルの「インデックス」タブ",
          "その表の索引の一覧。右クリックから CREATE INDEX 文も見られる",
        ],
        [
          "EXPLAIN のノード名",
          "Index Scan / Index Only Scan / Bitmap Index Scan なら使われている。Seq Scan なら使われていない",
        ],
        [
          "pg_stat_user_indexes の idx_scan",
          "その索引が何回使われたか。ずっと0なら、使われていない索引",
        ],
        [
          "pg_relation_size(索引名)",
          "索引の大きさ。表より大きい索引は作りすぎの合図",
        ],
      ],
    },
  },
];

// ---------- SQLite ----------

const SQLITE: HelpSection[] = [
  INTRO,
  {
    title: "まず rowid の話",
    table: {
      head: ["こと", "意味"],
      rows: [
        [
          "すべての行に rowid がある",
          "隠れた連番で、行の置き場所そのもの。rowid で引くのがいちばん速い",
        ],
        [
          "INTEGER PRIMARY KEY は rowid",
          "この型で主キーを書くと、rowid そのものになります。別に索引を作る必要はありません",
        ],
        [
          "それ以外の主キーは索引",
          "文字列の主キーなどは、rowid とは別に索引が作られます (引いたあと rowid で行を取りに行きます)",
        ],
        [
          "WITHOUT ROWID",
          "主キーの順に行そのものを並べる作り方。文字列の主キーで引くことが多い表では速くなります",
        ],
      ],
    },
  },
  {
    title: "作れる種類",
    table: {
      head: ["書き方", "意味"],
      rows: [
        ["CREATE INDEX", "ふつうの索引。重複してよい"],
        ["CREATE UNIQUE INDEX", "重複を許さない索引"],
        [
          "(a, b, c)",
          "複合索引。左から順にしか使えない (a / a,b / a,b,c)。b だけでは使えない",
        ],
        [
          "... WHERE 条件",
          "部分索引。条件に合う行だけを索引にする (下の落とし穴にも注意)",
        ],
        [
          "(lower(name))",
          "式索引。WHERE lower(name) = ... と、同じ式で書いたときに使える",
        ],
        [
          "(t COLLATE NOCASE)",
          "大文字小文字を区別しない索引。引くときも同じ照合順序で書く必要があります",
        ],
        ["(a DESC)", "大きい順の索引。ORDER BY ... DESC の並べ替えを省ける"],
      ],
    },
  },
  {
    title: "索引が効かなくなる書き方",
    table: {
      head: ["書き方", "どうなるか / どう直すか"],
      rows: [
        [
          "WHERE date(created_at) = '2026-01-01'",
          "列に関数を掛けると効かない。範囲の条件に書き換えるか、式索引を作る",
        ],
        [
          "WHERE name LIKE '%abc%'",
          "中間一致は効かない。前方一致 'abc%' にするか、FTS5 (全文検索) を検討する",
        ],
        [
          "照合順序が合っていない",
          "COLLATE NOCASE の索引は、WHERE 側も COLLATE NOCASE と書く (または列の定義に付ける) まで使われません",
        ],
        [
          "部分索引の条件が書かれていない",
          "WHERE b > 100 の部分索引は、b > 200 だけでは使われません。b > 100 AND b < 150 のように索引の条件を書くと使われます",
        ],
        [
          "AUTOMATIC INDEX と出る",
          "その場限りの索引を毎回作っている合図。同じ形の索引を本物として作る",
        ],
      ],
    },
  },
  {
    title: "カバリングインデックス",
    lines: [
      "要る列がすべて索引の中にあると、表本体を読まずに済みます (EXPLAIN に USING COVERING INDEX と出ます)。",
      "SQLite に INCLUDE はないので、持たせたい列も索引の列として並べます。例えば WHERE a = ? で b を返したいなら (a, b) の索引にします。",
      "並べる順番は、絞り込みに使う列を先に、持たせたいだけの列を後ろにします。",
    ],
  },
  {
    title: "統計 (ANALYZE)",
    lines: [
      "SQLite の ANALYZE は、表と索引の統計を集めて sqlite_stat1 に保存する命令です (実行計画を出す命令ではありません)。",
      "統計があると、どの索引を使うか・結合でどちらを外側にするかの判断が良くなります。",
      "データを大きく入れ替えたあとに一度流しておくと、計画が安定します。",
    ],
  },
  {
    title: "確かめ方",
    table: {
      head: ["やること", "分かること"],
      rows: [
        [
          "テーブルの「インデックス」タブ",
          "その表の索引の一覧。右クリックから CREATE INDEX 文も見られる",
        ],
        [
          "EXPLAIN (実行計画)",
          "SEARCH ... USING INDEX なら使われている。SCAN なら全件読み",
        ],
        ["PRAGMA index_list(表名)", "その表の索引の一覧 (部分索引かどうかも分かる)"],
        ["PRAGMA index_info(索引名)", "その索引が、どの列をどの順で持っているか"],
      ],
    },
  },
];

export const INDEX: HelpTopic = {
  id: "index",
  label: "インデックス",
  note: "種類と、効く条件・効かない書き方",
  // Valkey は key そのものが索引にあたるので、この話は当てはまらない
  dbTypes: ["mysql", "postgresql", "sqlite"],
  sections: (dbType) => {
    if (dbType === "postgresql") return POSTGRESQL;
    if (dbType === "sqlite") return SQLITE;
    return MYSQL;
  },
};
